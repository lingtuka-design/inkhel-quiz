# ============================================================
# Inkhel — Environment Variables
# ------------------------------------------------------------
# Public (bundled into the client build — NEVER put secrets here)
# ============================================================
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ============================================================
# Server-only — NEVER expose in the frontend.
# Used by Supabase Edge Functions / backend workers.
# ============================================================
SUPABASE_SERVICE_ROLE_KEY=
ADMIN_AUTH_SECRET=

# Demo admin (used by the local dev datastore):
#   username: admin
#   password: admin123
# Change this before any real deployment.
